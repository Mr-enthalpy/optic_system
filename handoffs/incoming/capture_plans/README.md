# Incoming Capture Plan Handoffs

Reserved for future capture-plan handoffs from `LCD_forward` or
`reconstruction`.

This directory is intentionally non-functional. It reserves a future handoff
boundary only.

Rules:

- do not import `LCD_forward` or `reconstruction`;
- do not implement a capture-plan parser here;
- do not define final external capture-plan schemas here;
- do not commit generated large artifacts;
- real data should live in third-party storage or ignored output paths;
- commit only small manifests/spec examples when appropriate.
