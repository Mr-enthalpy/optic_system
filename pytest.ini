[pytest]
testpaths = tests
markers =
    hardware: requires real FlyCapture2 SDK and camera hardware
    phase2_hardware: requires real camera, LCD, and optionally TLS hardware
    phase3_hardware: requires real camera, LCD, and optionally TLS hardware for Phase 3
