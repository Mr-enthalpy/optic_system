# Legacy Phase 3.1 Coarse Pupil Scan

These files are retained only as historical PR #24 artifacts.

They were produced with the revoked coarse `camera_params.json` exposure
selection and are not downstream-usable for Phase 3.1.1, dOTF, PSF dictionary,
PSF repeatability, or pupil-fit work.

Do not use this directory as an operational input. New Phase 3 captures must
use `outputs/exposure_calibration/camera_params_psf_safe.json`.

