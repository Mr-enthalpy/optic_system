# AGENTS.md

Canonical project instructions live in [AGENTS/AGENTS.md](AGENTS/AGENTS.md).

Use that file as the single source of truth for:

- architecture boundaries;
- camera startup behavior;
- LCD integration rules;
- stage scope and success criteria.

The stage-specific documents under `AGENTS/` remain as historical references, but new work should follow `AGENTS/AGENTS.md`.
